<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="saeut.dao.mybatis.mapper.DemandMapper">
  <cache />
  
	  <select id="getDemandByUserId" resultType="Demand">
	    select ranks, score, description, type, id 
		from Demand 
		where id = #{id}
	  </select>
	  
	  <select id="getAllDemand" resultType="Demand">
	    select ranks, score, description, type, id  
		from Demand
	  </select>
	  
	   <insert id="insertDemand"  parameterType="Demand">
		 insert into Demand(ranks, score, description, type, id)
		 values(#{ranks}, #{score}, #{description}, #{type}, #{id})
		</insert>
	
   	  <update id="modDemand"  parameterType="Demand">
	     update Demand
	     set rank=#{ranks}, score=#{score}, description=#{description}, type=#{type}
	     where id=#{id}
   	  </update> 
     
      <delete id="removeDemand"  parameterType="String">
	   delete from Demand
	   where
	   id=#{id}
	  </delete>
	  
  
  </mapper>